
export enum CampaingState{
    CREATED = 'created',
    INPROCESS = 'inprocess',
    COMPLETED = 'completed',
    FAILED = 'failed'
}

export enum MessageState{
    CREATED = 'created',
    INPROCESS = 'inprocess',
    COMPLETED = 'completed',
    FAILED = 'failed'
}

